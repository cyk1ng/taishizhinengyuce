# Windows兼容版依赖 - 使用预编译包
# 此版本跳过需要编译的包，或使用预编译的wheel

# LangChain 核心
langchain==1.0.3
langchain-core==1.0.2
langchain-openai==1.0.1
langgraph==1.0.2
langgraph-checkpoint==3.0.0
langgraph-prebuilt==1.0.2

# Coze SDK
coze-coding-dev-sdk==0.5.11
coze-coding-utils==0.2.4
coze-workload-identity==0.1.4

# 数据处理 - 使用预编译版本
# 注意：不指定版本，让pip自动选择兼容的预编译包
pandas
numpy

# HTTP请求
requests
httpx

# FastAPI服务
fastapi
uvicorn

# 工具库
python-dotenv
pydantic

# 数据库（如果需要）
psycopg2-binary
